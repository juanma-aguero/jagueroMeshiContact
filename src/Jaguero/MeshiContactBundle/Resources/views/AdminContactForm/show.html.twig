{% extends "FlowcodeDashboardBundle:Default:layout.html.twig" %}

{% block title %}ContactForm - {{ parent() }}{% endblock %}

{% block body %}
    <section class="content-header">
        <h1>
            <a href="{{ path('admin_contactform') }}"> {{ 'ContactForm list'|trans({'ContactForm': 'ContactForm'}, 'ContactForm') }}</a>
            /
            {{ 'ContactForm' | trans({}, 'ContactForm')}}
        </h1>
    </section>
    <section class="content">    
        <div class="box"  id='showContactForm'>
            <div class="box-header">
                <div class="box-tools  pull-right">
                            {{ form_start(delete_form) }}
        {{ form_widget(delete_form) }}

        <div class="btn-group record_actions">            <button class="btn btn-link btn-sm" type="submit"> {{ 'Delete'|trans({}, 'ContactForm') }}</button>
            <input name="modal" type="hidden" value="0">
            
                <a class="btn btn-link  btn-sm" onclick="showEdit(true)">
                    {{ 'Edit'|trans({}, 'ContactForm') }}
                </a>
</div>
            <input name="modal" type="hidden" value="0">
        {{ form_end(delete_form) }}

</div>
            </div>
            <div class="box-body">
                <table class="table table-hover record_properties">
                    <tbody>                            
                        <tr>
                            <th scope="row">{{'Name'|trans({}, 'ContactForm')}}</th>
                            <td>{{ contactform.name }}</td>
                        </tr>                            
                        <tr>
                            <th scope="row">{{'Yourname'|trans({}, 'ContactForm')}}</th>
                            <td>{{ contactform.yourName }}</td>
                        </tr>                            
                        <tr>
                            <th scope="row">{{'Youremail'|trans({}, 'ContactForm')}}</th>
                            <td>{{ contactform.yourEmail }}</td>
                        </tr>                            
                        <tr>
                            <th scope="row">{{'Subject'|trans({}, 'ContactForm')}}</th>
                            <td>{{ contactform.subject }}</td>
                        </tr>                            
                        <tr>
                            <th scope="row">{{'Yourmessage'|trans({}, 'ContactForm')}}</th>
                            <td>{{ contactform.yourMessage }}</td>
                        </tr>                            
                        <tr>
                            <th scope="row">{{'Redirecturl'|trans({}, 'ContactForm')}}</th>
                            <td>{{ contactform.redirectUrl }}</td>
                        </tr>                            
                        <tr>
                            <th scope="row">{{'Toemail'|trans({}, 'ContactForm')}}</th>
                            <td>{{ contactform.toEmail }}</td>
                        </tr>
                    </tbody>
                </table>
            </div>
            <div class="box-footer">
            
            </div>
        </div>
        <div class="box" id='editFormContactForm'  style="display: none;">
            <div class="box-header">
                <div class="box-tools">
                    {{ form_start(delete_form) }}
                    {{ form_widget(delete_form) }}
                    <div class="btn-group record_actions">
                        <button class="btn btn-link btn-sm" type="submit"> {{ 'Delete'|trans({}, 'ContactForm') }}</button>
                        <a class="btn btn-link  btn-sm" onclick="showEdit(false)">
                        {{ 'Cancel'|trans({}, 'Account') }}
                        </a>
                    </div>
                    <input name="modal" type="hidden" value="0">
                    {{ form_end(delete_form) }}
                </div>
            </div>
            <div class="box-body">

            {{ form_start(edit_form) }}
                {{ form_widget(edit_form) }}
                <p>
                    <button class="btn-default btn" type="submit">{{ 'save'|trans | capitalize}}</button>
                </p>
            {{ form_end(edit_form) }}
            </div>
            <div class="box-footer">
            </div>
        </div>
    </section>

        <div class="btn-group record_actions"></div>

        <div id="delete_confirm" class="modal fade">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-header">
                        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                        <h3>{{ 'Confirm delete'|trans({}, 'ContactForm') }}</h3>
                    </div>
                    <div class="modal-body">
                        <p>{{ 'You are about to delete an item'|trans({}, 'ContactForm') }}, {{ 'this procedure is irreversible'|trans({}, 'ContactForm') }}.</p>
                        <p>{{ 'Do you want to proceed?'|trans({}, 'ContactForm') }}</p>
                    </div>
                    <div class="modal-footer">
                        <button class="btn btn-danger">{{ 'Yes'|trans({}, 'ContactForm') }}</button>
                        <button class="btn btn-default" data-dismiss="modal">{{ 'No'|trans({}, 'ContactForm') }}</button>
                    </div>
                </div>
            </div>
        </div>
{% endblock %}
{% block javascripts %}

    {{ parent() }}
    <script type="text/javascript">
        function showEdit(show){
            if(show){
                $('#showContactForm').hide();
                $('#editFormContactForm').show();
            }else{
                $('#showContactForm').show();
                $('#editFormContactForm').hide();
            }
        }
        
    </script>
{% endblock %}